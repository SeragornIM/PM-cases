# API Документация

## Обзор API

Телеграм-бот «ПетербургГаз» использует несколько API для своей работы:

1. **Telegram Bot API** - для взаимодействия с пользователями
2. **GigaChat API** - для генерации умных ответов
3. **Внутренние API** - для поиска в FAQ и парсинга сайта

## Telegram Bot API

### Команды

#### `/start`
- **Описание:** Приветствие и инструкции для пользователя
- **Доступность:** Все пользователи
- **Ответ:** Приветственное сообщение с инструкциями

#### `/stats`
- **Описание:** Статистика использования бота
- **Доступность:** Только администраторы (ID в `ADMIN_IDS`)
- **Ответ:** Детальная статистика в текстовом формате

### Обработка сообщений

#### Текстовые сообщения
- **Тип:** Любое текстовое сообщение
- **Обработка:** 
  1. Проверка на аварийность
  2. Проверка на оффтоп
  3. Поиск контекста
  4. Генерация ответа через GigaChat
  5. Отправка ответа с кнопками оценки (для тематических вопросов)

#### Callback кнопки
- **Тип:** Нажатие на кнопки 👍/👎
- **Формат:** `rating_{message_id}_{rating}`
- **Обработка:** Сохранение оценки в базу данных

## GigaChat API

### Аутентификация

Используется OAuth 2.0 с Basic Authentication:

```http
POST https://ngw.devices.sberbank.ru:9443/api/v2/oauth
Authorization: Basic {base64(Client ID:Client Secret)}
Content-Type: application/x-www-form-urlencoded

scope=GIGACHAT_API_PERS
```

### Получение токена доступа

```http
POST https://ngw.devices.sberbank.ru:9443/api/v2/oauth
Authorization: Basic {base64_encoded_credentials}
Content-Type: application/x-www-form-urlencoded

scope=GIGACHAT_API_PERS
```

**Ответ:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "expires_at": 1234567890
}
```

### Генерация ответа

```http
POST {GIGACHAT_BASE_URL}/api/v1/chat/completions
Authorization: Bearer {access_token}
Content-Type: application/json

{
  "model": "GigaChat-2",
  "messages": [
    {
      "role": "system",
      "content": "Системный промпт с ограничениями по тематике"
    },
    {
      "role": "user", 
      "content": "Пользовательский запрос с контекстом"
    }
  ],
  "temperature": 0.2,
  "max_tokens": 1000
}
```

**Ответ:**
```json
{
  "choices": [
    {
      "message": {
        "content": "Сгенерированный ответ",
        "role": "assistant"
      }
    }
  ],
  "usage": {
    "prompt_tokens": 150,
    "completion_tokens": 200,
    "total_tokens": 350
  }
}
```

### Получение доступных моделей

```http
GET {GIGACHAT_BASE_URL}/api/v1/models
Authorization: Bearer {access_token}
```

**Ответ:**
```json
{
  "data": [
    {
      "id": "GigaChat-2",
      "object": "model",
      "created": 1234567890,
      "owned_by": "sberbank"
    }
  ]
}
```

## Внутренние API

### FAQ Retriever

#### Поиск в FAQ
```python
def search_faq(query: str, top_k: int = 5, threshold: float = 75.0) -> List[Dict]
```

**Параметры:**
- `query` - поисковый запрос
- `top_k` - количество результатов (по умолчанию 5)
- `threshold` - минимальный порог схожести (по умолчанию 75.0)

**Возвращает:**
```python
[
  {
    "question": "Вопрос из FAQ",
    "answer": "Ответ из FAQ", 
    "score": 85.5,
    "source": "faq"
  }
]
```

### Site Retriever

#### Парсинг сайта
```python
def search_site(query: str, top_k: int = 3) -> List[Dict]
```

**Параметры:**
- `query` - поисковый запрос
- `top_k` - количество результатов (по умолчанию 3)

**Возвращает:**
```python
[
  {
    "title": "Заголовок страницы",
    "content": "Содержимое страницы",
    "url": "https://peterburggaz.ru/page",
    "score": 78.2,
    "source": "site"
  }
]
```

## База данных API

### DatabaseManager

#### Добавление пользователя
```python
def add_user(user_id: int, username: str = None, first_name: str = None) -> None
```

#### Логирование сообщения
```python
def log_message(user_id: int, message: str, response: str, 
                context_source: str, gigachat_used: bool = False) -> int
```

**Возвращает:** ID сообщения в базе данных

#### Установка оценки
```python
def set_rating(message_id: int, rating: int) -> None
```

**Параметры:**
- `message_id` - ID сообщения
- `rating` - оценка (1 для 👍, 0 для 👎)

#### Получение статистики
```python
def get_admin_stats() -> Dict
```

**Возвращает:**
```python
{
  "total_users": 150,
  "users_this_month": 25,
  "total_messages": 1200,
  "messages_today": 15,
  "messages_this_week": 85,
  "messages_this_month": 300,
  "ratings": {
    "likes": 450,
    "dislikes": 50
  },
  "top_questions": [
    {"question": "Как подать заявку?", "count": 25},
    {"question": "Сколько стоит подключение?", "count": 20}
  ],
  "sources": {
    "faq": 600,
    "site": 300,
    "site+faq": 200,
    "none": 100
  }
}
```

## Обработка ошибок

### Типы ошибок

#### Telegram API ошибки
- `Conflict` - несколько экземпляров бота запущено одновременно
- `Unauthorized` - неверный токен бота
- `BadRequest` - некорректный запрос

#### GigaChat API ошибки
- `401 Unauthorized` - неверный API ключ или истекший токен
- `403 Forbidden` - недостаточно прав доступа
- `429 Too Many Requests` - превышен лимит запросов
- `500 Internal Server Error` - внутренняя ошибка сервера

#### Внутренние ошибки
- `FileNotFoundError` - отсутствует файл FAQ
- `JSONDecodeError` - некорректный формат FAQ
- `SQLiteError` - ошибка базы данных
- `ConnectionError` - проблемы с сетевым соединением

### Обработка ошибок

Все ошибки логируются с соответствующим уровнем:
- `ERROR` - критические ошибки
- `WARNING` - предупреждения
- `INFO` - информационные сообщения
- `DEBUG` - отладочная информация

### Fallback механизмы

1. **GigaChat недоступен** - используется только FAQ поиск
2. **FAQ недоступен** - используется только парсинг сайта
3. **Оба источника недоступны** - отправляется сообщение о временной недоступности

## Лимиты и ограничения

### Telegram Bot API
- 30 сообщений в секунду
- 20 сообщений в минуту на группу
- 1 сообщение в секунду на пользователя

### GigaChat API
- 1000 запросов в день (зависит от тарифа)
- 1000 токенов на запрос
- Таймаут 30 секунд

### Внутренние ограничения
- Максимум 4000 символов контекста
- Максимум 1200 символов из FAQ
- Максимум 1500 символов с сайта
- Таймаут 30 секунд на внешние запросы

## Мониторинг и метрики

### Логируемые события
- Все входящие сообщения
- Все исходящие ответы
- Использование токенов GigaChat
- Время обработки запросов
- Источники контекста
- Оценки пользователей

### Метрики производительности
- Среднее время ответа
- Процент успешных запросов
- Использование токенов
- Популярность вопросов
- Эффективность поиска

## Безопасность

### Защита данных
- Все API ключи хранятся в переменных окружения
- Параметризованные SQL запросы
- Валидация всех входящих данных
- Экранирование специальных символов

### Ограничения доступа
- Команда `/stats` только для администраторов
- Парсинг ограничен доменом peterburggaz.ru
- Таймауты для всех внешних запросов
- Логирование без секретных данных
