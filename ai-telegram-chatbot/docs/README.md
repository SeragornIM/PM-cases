# Телеграм-бот «ПетербургГаз»

Интеллектуальный помощник для ответов на вопросы о газоснабжении с использованием GigaChat и локального FAQ.

## 🚀 Возможности

- **Умные ответы** на основе GigaChat с контекстом из FAQ и официального сайта
- **Аварийные уведомления** при обнаружении ключевых слов об утечке газа
- **Обработка оффтопных вопросов** с вежливым перенаправлением на тематику
- **Поиск по FAQ** с использованием fuzzy-поиска и TF-IDF
- **Парсинг сайта** peterburggaz.ru для актуальной информации
- **Система оценок** с кнопками 👍/👎 для обратной связи (только для тематических вопросов)
- **Административная статистика** через команду /stats
- **Логирование** всех взаимодействий в SQLite

## 📋 Требования

- Python 3.8+
- Telegram Bot Token
- GigaChat API ключ и URL
- Файл FAQ в формате JSON

## 🛠 Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd Telegabot
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv .venv
.venv\Scripts\activate  # Windows
# или
source .venv/bin/activate  # Linux/Mac
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте конфигурацию:**
   
   Создайте файл `.env` в корне проекта:
```bash
# Токен Telegram бота (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_token

# API ключ GigaChat (Base64 encoded Client ID:Client Secret)
GIGACHAT_API_KEY=your_gigachat_key

# URL API GigaChat (БЕЗ /api/v1 на конце)
GIGACHAT_BASE_URL=https://gigachat.devices.sberbank.ru

# Модель GigaChat
GIGACHAT_MODEL=GigaChat-2

# ID администраторов через запятую (для команды /stats)
ADMIN_IDS=123456789,987654321

# Путь к FAQ файлу
FAQ_PATH=faq.json

# Путь к базе данных
DB_PATH=bot.db

# Таймаут запросов (секунды)
REQUEST_TIMEOUT=30

# Максимальная длина контекста
MAX_CONTEXT_LENGTH=4000
```

5. **Добавьте файл FAQ:**
   
   Создайте файл `faq.json` в корне проекта со структурой:
```json
[
  {
    "question": "Как подать заявку на подключение газа?",
    "answer": "Для подачи заявки на подключение газа необходимо..."
  },
  {
    "question": "Сколько стоит подключение газа?",
    "answer": "Стоимость подключения газа зависит от..."
  }
]
```

## 🚀 Запуск

```bash
python run_bot.py
```

## 📁 Структура проекта

```
Telegabot/
├── bot.py                 # Основной файл бота
├── config.py              # Конфигурация и настройки
├── constants.py           # Константы и ключевые слова
├── storage.py             # Работа с SQLite базой данных
├── gigachat_client.py     # Клиент для GigaChat API
├── retrievers.py          # Поиск в FAQ и парсинг сайта
├── run_bot.py             # Точка входа для запуска бота
├── requirements.txt       # Зависимости Python
├── env_example.txt        # Пример файла конфигурации
├── docs/                  # Документация проекта
│   ├── README.md         # Основная документация
│   ├── API.md            # API документация
│   ├── DEPLOYMENT.md     # Руководство по развертыванию
│   └── TROUBLESHOOTING.md # Устранение неполадок
├── faq.json              # Файл FAQ (создается пользователем)
└── bot.db                # База данных SQLite (создается автоматически)
```

## ⚙️ Конфигурация

### Основные параметры в `.env`:

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `GIGACHAT_API_KEY` - API ключ GigaChat (Base64 encoded)
- `GIGACHAT_BASE_URL` - URL API GigaChat
- `GIGACHAT_MODEL` - модель GigaChat для использования
- `ADMIN_IDS` - список ID администраторов для команды /stats
- `FAQ_PATH` - путь к файлу FAQ (по умолчанию "faq.json")
- `DB_PATH` - путь к базе данных (по умолчанию "bot.db")

### Параметры поиска в `constants.py`:

- `EMERGENCY_KEYWORDS` - ключевые слова для аварийных ситуаций
- `OFFTOP_KEYWORDS` - ключевые слова для определения оффтопа
- `SEARCH_SETTINGS` - настройки поиска (пороги, лимиты)
- `CONTEXT_LIMITS` - лимиты контекста для GigaChat

## 🤖 Команды бота

- `/start` - приветствие и инструкции
- `/stats` - статистика (только для администраторов)

## 📊 Статистика

Команда `/stats` показывает:
- Количество пользователей (всего и за месяц)
- Количество сообщений по периодам
- Статистику оценок (лайки/дизлайки)
- Топ-5 популярных вопросов
- Статистику по источникам ответов

## 🔍 Обработка вопросов

### Тематические вопросы
- Поиск контекста в FAQ и на сайте
- Генерация ответа через GigaChat
- Отображение кнопок оценки 👍/👎

### Оффтопные вопросы
- Вежливое перенаправление на тематику газоснабжения
- Отсутствие кнопок оценки
- Информативные подсказки

### Аварийные ситуации
- Немедленная отправка инструкций по безопасности
- Приоритетная обработка

## 🚨 Аварийные ситуации

Бот автоматически определяет аварийные ситуации по ключевым словам:
- "утечка газа", "запах газа", "взрыв", "прорыв трубы"
- И другие связанные термины

При обнаружении аварийной ситуации бот немедленно отправляет инструкции по безопасности.

## 🔍 Поиск информации

1. **Поиск в FAQ** - использует RapidFuzz и TF-IDF для точного поиска
2. **Парсинг сайта** - обходит peterburggaz.ru для актуальной информации
3. **Контекст** - объединяет результаты для максимально точного ответа

## 🛡️ Безопасность

- Все запросы к внешним API имеют таймауты
- Ограниченный парсинг только домена peterburggaz.ru
- Валидация всех входящих данных
- Защита от SQL-инъекций через параметризованные запросы
- OAuth 2.0 аутентификация для GigaChat API

## 📝 Логи

Логи записываются в консоль с уровнем INFO. Для изменения уровня логирования отредактируйте `logging.basicConfig` в `bot.py`.

## 🐛 Устранение неполадок

### Бот не запускается:
1. Проверьте правильность токенов в `.env`
2. Убедитесь, что установлены все зависимости
3. Проверьте наличие файла `faq.json`

### GigaChat не отвечает:
1. Проверьте правильность `GIGACHAT_API_KEY` и `GIGACHAT_BASE_URL`
2. Убедитесь, что API ключ активен и правильно закодирован
3. Проверьте сетевое соединение

### FAQ не загружается:
1. Проверьте формат JSON в файле `faq.json`
2. Убедитесь, что файл находится в корне проекта
3. Проверьте кодировку файла (должна быть UTF-8)

### Ошибка команды /stats:
1. Убедитесь, что ваш ID добавлен в `ADMIN_IDS`
2. Проверьте формат ID (только цифры, через запятую)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь в корректности конфигурации
3. Проверьте доступность внешних сервисов
4. Обратитесь к документации в папке `docs/`

## 📄 Лицензия

Проект создан для внутреннего использования компании ПетербургГаз.
